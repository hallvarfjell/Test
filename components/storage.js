const Storage={ load(k,def){ try{ const v=localStorage.getItem('INTZ_'+k); return v? JSON.parse(v):def;}catch(e){return def;} }, save(k,v){ try{ localStorage.setItem('INTZ_'+k, JSON.stringify(v)); }catch(e){} }, loadP(p,k,def){ return this.load(p+':'+k, def); }, saveP(p,k,v){ return this.save(p+':'+k, v); } };
