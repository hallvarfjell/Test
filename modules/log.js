const LogMod={ render(el,st){ el.innerHTML=''; const list = UI.h('div',{class:'list'}); (st.logg||[]).slice().reverse().forEach(s=>{ list.appendChild(UI.h('div',{class:'list-item'}, `${new Date(s.endedAt).toLocaleString()} – ${s.name}`));}); el.append(UI.h('h1',{class:'h1'},'Logg'), list); } };
